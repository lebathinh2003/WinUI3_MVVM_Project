<Page
    x:Class="WinUI3_MVVM_Project.Views.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3_MVVM_Project.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WinUI3_MVVM_Project.ViewModels"
    xmlns:converters="using:WinUI3_MVVM_Project.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <StackPanel Spacing="10" MaxWidth="500">
            <TextBlock Text="Thông tin cá nhân" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>

            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          HorizontalAlignment="Center"
                          Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Margin="0,0,0,10"/>

            <Grid Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}">
                <StackPanel Spacing="5" Visibility="{x:Bind ViewModel.CurrentUser, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="ID người dùng:" FontWeight="SemiBold"/>
                    <TextBlock Text="{x:Bind ViewModel.CurrentUser.Id, Mode=OneWay}"/>

                    <TextBlock Text="Tên đăng nhập:" FontWeight="SemiBold" Margin="0,10,0,0"/>
                    <TextBlock Text="{x:Bind ViewModel.CurrentUser.Username, Mode=OneWay}"/>

                    <TextBlock Text="Họ và tên:" FontWeight="SemiBold" Margin="0,10,0,0"/>
                    <TextBlock Text="{x:Bind ViewModel.CurrentUser.FullName, Mode=OneWay}"/>

                    <TextBlock Text="Email:" FontWeight="SemiBold" Margin="0,10,0,0"/>
                    <TextBlock Text="{x:Bind ViewModel.CurrentUser.Email, Mode=OneWay}"/>
                </StackPanel>

                <TextBlock Text="Không thể tải thông tin người dùng hoặc chưa có người dùng đăng nhập."
                           Visibility="{x:Bind ViewModel.CurrentUser, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=True}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontStyle="Italic" TextWrapping="Wrap"/>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
